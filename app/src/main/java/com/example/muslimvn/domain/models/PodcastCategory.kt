package com.example.muslimvn.domain.models

/** Nhóm phân loại học giả, ví dụ ("tafsir", "Giải Thích Qur'an") — đọc từ scholars.json. */
data class PodcastCategory(
    val id: String,
    val name: String
)
