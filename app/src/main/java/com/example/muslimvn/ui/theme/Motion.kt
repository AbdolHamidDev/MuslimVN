package com.example.muslimvn.ui.theme

/**
 * Các token thời lượng Animation theo chuẩn Material Design 3 Motion.
 */
object MuslimVNMotion {
    /** Phản hồi nhanh (150ms) cho tương tác nhỏ như toggle, switch, icon press */
    const val DURATION_SHORT = 150

    /** Chuyển cảnh vừa (300ms) cho chuyển màn hình, mở rộng layout, fade in/out */
    const val DURATION_MEDIUM = 300

    /** Hiệu ứng dài (500ms) cho chuyển đổi bố cục phức tạp hoặc hiệu ứng đếm số */
    const val DURATION_LONG = 500
}
